<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.groom.backend.member.MemberDAO">
	<insert id="signup">
  		insert into member(user_id, user_pwd, user_phone, user_birth, user_name) 
  		values(#{user_id}, #{user_pwd}, #{user_phone}, #{user_birth}, #{user_name})
  	</insert>
</mapper>